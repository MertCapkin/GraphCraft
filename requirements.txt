# GraphStack runtime dependencies.
#
# Only one external package is required: Graphify, the local AST-only graph
# engine that powers /graphify .  /graphify --update commands.
#
# Pin range explained:
#   - Lower bound 0.7 — first Graphify line GraphStack v3/v4 were validated against.
#   - Upper bound 0.9 — pre-1.0 minor bumps may break compatibility; this
#     ceiling lets 0.7.x and 0.8.x patches through, but blocks unverified
#     future releases. Update once a newer minor has been smoke-tested.
graphifyy>=0.7,<0.9
