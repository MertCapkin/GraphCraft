name: graphcraft_ui_core
description: GraphCraft Flutter UI primitives — maps to design graph component ids
version: 0.6.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
